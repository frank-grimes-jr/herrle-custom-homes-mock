# Copy to .env.local and fill in to turn the email triage briefs into live,
# Claude-generated summaries. Without it, the app renders the deterministic
# sample briefs (no API calls). .env.local is gitignored by default.
ANTHROPIC_API_KEY=
